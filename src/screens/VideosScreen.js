import { View, Text } from 'react-native';
export default function VideosScreen(){ 
    return(
    <View style={{flex:1,justifyContent:'center',alignItems:'center'}}>
        <Text>Lướt video</Text></View>
    ); 
}